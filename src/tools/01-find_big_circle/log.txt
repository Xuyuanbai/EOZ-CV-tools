# V1 和 V2 是两种不同的算法，没有任何相似性，v1到v2并不是简单的修bug。

# V1 基于色彩饱和运算寻找外圆
    V1.0 基于色彩饱和运算寻找外圈但识别受坐标文字的干扰产生刺突 file output_spike_mask.png ，无法通过直接包络（凸包）实现外圆寻找。
    v1.1 通过刺突样黑白mask基于对距离先后取最大与最小获得圆心和半径。但是因为膨胀与腐蚀导致mask内缩人为膨胀了7像素后得到接近准确的值，这个方法对另一批的地形图不一定有效。

# V2 通过win规范截图所得的图案外圈灰色为（128，128，128）尝试基于此色彩获取外圆，但需要解决外界的干扰。
    V2.0 目前可以找出：“圆环+比例尺+放大镜”的柄的轮廓
    V2.1 圆的拟合：找到圆，解决外界的干扰

Timeline
2026-01-15 输出 V1.1为 01BigCircle